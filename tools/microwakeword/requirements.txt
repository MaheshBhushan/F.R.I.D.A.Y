# Training-only dependencies. Install into tools/microwakeword/.venv, never
# FRIDAY's production .venv. Git revisions match the Phase 1 investigation.
microWakeWord @ git+https://github.com/OHF-Voice/micro-wake-word.git@4665173cd35f1cff9a61e06fc427f124766c488e
# Upstream uses AddColorNoise but leaves this dependency unbounded; its resolver
# otherwise selects Piper-compatible 0.33, where that transform does not exist.
audiomentations==0.43.1
# 5.x changed Audio decoding to require torchcodec; upstream Clips expects the
# direct NumPy decoder used by the 2.x API.
datasets==2.21.0
# Upstream invokes tf.summary at every evaluation checkpoint but omits the
# TensorBoard implementation from its package metadata.
tensorboard==2.20.0

# Tooling used by the project-owned scripts and tests.
pytest==9.1.1
PyYAML==6.0.3
# Legacy `webrtcvad` imported by Piper still imports pkg_resources, removed
# from newer setuptools releases.
setuptools==80.9.0
