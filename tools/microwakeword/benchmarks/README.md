# Wake benchmark results

Store immutable corpus manifests and raw per-frame/per-invocation JSON results
here. Summary tables must be generated from these files. Both wake backends
must receive the same PCM bytes and sequence numbers.
